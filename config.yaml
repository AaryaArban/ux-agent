project:
  name: "UX-Agent"
  version: "1.1.0"

model:
  log_input_dim: 10
  behavior_input_dim: 5
  text_model_name: "distilbert-base-uncased"
  fusion_hidden_dim: 128
  max_length: 128  # 🔧 Added to ensure text consistency

training:
  batch_size: 16
  learning_rate: 0.0001
  epochs: 20       # 🔧 Increased for better convergence
  device: "auto"

paths:
  raw_data: "data/raw/"
  processed_data: "data/processed/"
  screenshots: "data/screenshots/"
  models: "models/"
  logs: "logs/"
  scaler: "models/scaler.pkl" # 🔧 New: Path to save numerical scaling stats